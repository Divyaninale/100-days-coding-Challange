public class String_Compression
{
    public static void Compression(String str)
    {
        //char ch=str.charAt(i);
        int n=str.length();

        for(int i=0;i<n;i++)
        {
            if(str.charAt(i)==str.char))
        }
        if()
    }
    public static void main(String args[])
    {
        String str="aabb";
        Compression(str);
    }
}